export const MODIFIER = {
  SIZE: {
    SMALL: (Math.pow(0.4, 1/2) * 100).toString() +'%',
    MEDIUM: (Math.pow(0.7, 1/2) * 100).toString() +'%',
    LARGE: (Math.pow(1.0, 1/2) * 100).toString() +'%',
  },
  WIDTH: {
    SMALL: 1,
    MEDIUM: 1,
    LARGE: 1,
  },
  LENGTH: {
    SHORT: 2,
    MEDIUM: 4,
    LONG: 6,
  },
}